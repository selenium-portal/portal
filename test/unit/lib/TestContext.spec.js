var spec         = require('../../helpers/spec'),
    expect       = require('expect.js'),
    path         = require('path'),
    TestContext  = spec.require('lib/TestContext');

describe('TestContext', function () {
  var ctx;

  beforeEach(function () {
    ctx = new TestContext(spec.mock('portal.mock'));
  });

  it('', function () {
  });
});

