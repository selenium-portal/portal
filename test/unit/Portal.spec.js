var spec   = require('../helpers/spec'),
    expect = require('expect.js'),
    Portal = spec.require('Portal');

describe('Portal App', function () {
  beforeEach(function () {
    portal = new Portal();
  });
});

